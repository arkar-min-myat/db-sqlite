SELECT DISTINCT City
FROM customers
ORDER BY City;

SELECT City,Country
FROM customers
ORDER BY Country;

SELECT DISTINCT City,Country
FROM customers
ORDER BY Country;

SELECT Company
FROM customers;

SELECT DISTINCT Company
FROM customers
ORDER BY Company;